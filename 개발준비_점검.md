# 입실론 학생관리 v60 개발 준비 점검

점검일: 2026-09-03 (Asia/Seoul)

## 현재 기준본

- 배포 후보: `index.html` + `mobile-ui.css` + PWA 앱 셸
- 직전본: `index_v57_자료실.html`
- v59 변경: 2027학년도 9월 모평 점수 입력, 추정 등급컷, 예상 정시 배치표, 공식 성적표 정밀 모드 추가
- v60 변경: 휴대폰 하단 내비게이션, 터치 영역·안전 여백 개선, 앱 설치 안내, PWA 매니페스트·서비스워커·오프라인 셸 추가
- 기본 스키마: `supabase_schema.sql` (v45 역설계본)
- 추가 스키마: `files2/실전모의고사_supabase.sql`, `files2/팡팡포카_supabase.sql`
- Git, 패키지 관리자, 자동 테스트, 배포 설정은 없음

## 구현 범위

- D-day 및 개인 일정
- 수시 라인, 모의평가 성적 입력·추이·등급컷
- 대학 추천 및 지원 전략
- 학생/교사/원장 로그인과 반·계정 관리
- 시험 생성·배정·응시·채점·재채점·마감·랭킹
- OMR 이미지 판독(OpenCV)
- 오답 통계, 학생 리포트, 상담 화면
- 메달 및 팡팡포카 컬렉션
- 반별 자료실과 Supabase Storage 파일 관리

## 개발 차단 이슈 (먼저 처리)

1. **권한 상승 취약점**
   - `profiles_self_update`가 본인 행의 모든 컬럼 UPDATE를 허용한다.
   - 학생이 Supabase API로 자신의 `role`, `class_id`, `teacher_id` 등을 바꿀 수 있다.
   - 허용 컬럼을 RPC(`nickname`, `pw_changed` 등)로 제한하고 일반 UPDATE 정책을 제거해야 한다.

2. **반 변경 우회**
   - `set_my_profile`은 “최초 1회”라는 설명과 달리 반복 호출과 임의 반 선택을 막지 않는다.
   - 자료실이 반 기준으로 노출되므로 서버에서 최초 설정 조건과 허용 반을 검증해야 한다.

3. **스키마/클라이언트 불일치**
   - `submissions`는 `created_at`으로 정의되어 있으나 v58은 `submitted_at`을 조회한다.
   - 시험 결과 목록 쿼리가 실패할 수 있으므로 실제 운영 스키마를 확인한 뒤 한 이름으로 통일한다.

4. **백엔드 정의 누락**
   - 프론트가 쓰는 `teacher_update_key`, `test_wrong_matrix`, `my_wrong_history` RPC의 SQL이 로컬에 없다.
   - `viewer_groups`, `viewer_group_classes`, `viewer_materials`, `viewer` Storage bucket/RLS SQL도 없다.
   - 계정 생성·삭제·비밀번호 초기화용 `quick-action` Edge Function 소스도 없다.
   - 현재 파일만으로 새 Supabase 환경을 재현할 수 없다.

5. **저장형 XSS 가능성**
   - 사용자/교사가 입력하는 이름, 반명, 시험명, 자료명, 서버 오류 메시지를 여러 곳에서 문자열 템플릿으로 `innerHTML`에 넣는다.
   - DOM 생성 또는 공통 HTML escape 함수로 전환하고, URL·이미지 값도 허용 규칙을 적용해야 한다.

6. **비밀번호 취급**
   - 공통 초기 비밀번호 기본값이 화면 코드에 있으며 `initial_password`를 평문 저장·조회한다.
   - 일회용 임시 비밀번호, 최초 로그인 강제 변경, 평문 제거/만료 정책이 필요하다.

## 9월 모평·예상 배치표 신뢰도 정책

- 기본 **예상 모드**: 공개된 9월 가채점 등급컷 앵커 사이를 선형 보간해 예상 표준점수·백분위를 만들고 정시 배치표를 제공한다.
- **공식 정밀 모드**: 성적통지표의 표준점수·백분위를 직접 입력하면 예상값을 덮어쓰고 배치표를 다시 계산한다.
- 예상 모드 화면에는 `가채점 예상 모드`, `≈ 추정변환`, 지원 탐색용 경고를 상시 표시한다.
- 틀린 문항 및 문항별 정답률만으로 표준점수를 추정하는 기능은 구현하지 않는다. 전체 응시자 분포와 선택과목 조정 자료 없이는 신뢰도를 보장할 수 없기 때문이다.
- 국어·수학 9월 앵커는 공개 가채점 추정컷을 사용한다. 탐구 표준점수·백분위는 9월 원점수 추정컷에 6월 동일 등급 앵커를 결합한 참고값이므로 변동 위험이 더 크다.
- 배치자료는 2025 정시 공개 입결 기반 참고치이며, 대학별 최종 모집요강·환산식·가산점과 당해 지원자 이동까지 확정적으로 예측하지 않는다.

## 현재 미완성/시점성 항목

- 9월 모평 확정 성적 발표 후 `CUT2609`의 표준점수·백분위 앵커를 확정값으로 교체해야 한다.
- 입시/수시/대학 데이터가 대형 상수로 HTML에 내장되어 갱신 이력과 출처 검증이 어렵다.
- 상담 메뉴는 기본 내비게이션에서 숨겨져 있다.
- 운영 Supabase의 실제 마이그레이션 적용 상태와 RLS는 로컬 파일만으로 확인할 수 없다.

## 권장 개발 순서

### P0 — 배포 전 보안·정합성

1. 운영 스키마를 읽기 전용으로 덤프하여 로컬 SQL과 비교
2. 프로필 UPDATE/RPC 권한 수정 및 역할 상승 회귀 테스트
3. `created_at`/`submitted_at` 불일치 수정
4. 누락된 RPC, 자료실 스키마, Edge Function을 저장소에 복원
5. 입력값 escape/검증 공통화 및 저장형 XSS 테스트

### P1 — 재현 가능한 개발 환경

1. Git 저장소 생성 후 현재 v58을 기준 커밋으로 보존
2. `src/`, `data/`, `supabase/migrations/`, `supabase/functions/`, `tests/`로 분리
3. Supabase URL/anon key를 환경 설정으로 이동(서비스 키는 프론트에 두지 않음)
4. 로컬 실행 명령, 배포 절차, 롤백 절차를 README에 기록
5. ESLint/Prettier와 최소 브라우저 스모크 테스트 추가

### P2 — 유지보수성

1. 2.34 MB 단일 HTML을 기능 모듈과 정적 데이터로 분리
2. 187개 인라인 이벤트 핸들러와 126개 `innerHTML` 사용 축소
3. 입시 데이터 버전·출처·기준일을 별도 데이터 파일로 관리
4. CDN 의존성 버전 고정 및 장애 시 동작/안내 정의

## 확인 결과

- 인라인 JavaScript 3개 블록 구문 검사: 통과
- 로컬 HTTP 로딩: 통과
- 초기 D-day 화면과 입시/테스트/자료실 메뉴 전환: 통과
- 초기 브라우저 콘솔 경고·오류: 없음
- 9월 원점수 입력 → 추정 표준점수·백분위·등급 표시: 통과
- 추정값 저장 → 예상 정시 배치표 및 안정/적정/도전 구간 표시: 통과
- 공식 표준점수·백분위 입력 → 공식 정밀 모드 재계산: 통과
- 375×812 휴대폰 화면: 가로 넘침 없음, 주요 터치 영역 최소 44px
- 812×375 휴대폰 가로 화면: 하단 내비게이션과 본문 여백 정상
- 모바일 점수 입력 → 예상 정시 배치표: 통과
- 앱 매니페스트: 192px·512px·마스커블 512px 아이콘 확인
- 서비스워커 등록·제어 및 오프라인 앱 셸 재실행: 통과
- 동작 줄이기 설정과 125% 화면 확대: 가로 넘침 없음
- 로그인 이후 학생/교사/원장 흐름: 미검증(테스트 계정과 개발용 Supabase 필요)

## 다음 작업 시작 조건

- 운영 DB가 아닌 개발용 Supabase 프로젝트 또는 읽기 전용 운영 스키마 덤프
- 학생·교사·원장 테스트 계정 각 1개
- 우선 작업 선택: `P0 보안 패치` 권장
